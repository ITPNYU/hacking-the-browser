{
  "name": "timer",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.html",
    "deploy": "parcel build index.html && cp CNAME dist/ && cd dist/ && surge . && cd .."
  },
  "devDependencies": {
    "cssnano": "^4.1.10"
  }
}
