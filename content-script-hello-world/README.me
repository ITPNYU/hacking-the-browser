This extension uses the `<all_urls>` match pattern to automatically inject jquery.js
and content_script.js file into *every* page that the user browses to.

Read more about match patterns here:
  https://developer.chrome.com/extensions/match_patterns
